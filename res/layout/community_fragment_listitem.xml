<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:descendantFocusability="blocksDescendants"
    android:orientation="horizontal"
    android:paddingBottom="4dip"
    android:paddingLeft="6dip"
    android:paddingRight="6dip"
    android:paddingTop="10dip" >

    <ImageView
        android:id="@+id/community_listitem_userface"
        style="@style/listitem_image" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="6dip"
        android:layout_weight="1"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/community_listitem_title"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:text="fdgjfgsklgjls"
            android:textColor="@color/listitem_black"
            android:textSize="@dimen/text_size_15"
            android:textStyle="bold" />
        <!-- 摘要 -->

        <TextView
            android:id="@+id/community_listitem_Summary"
            android:layout_width="fill_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="6dp"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="2"
            android:textColor="@color/listitem_gray"
            android:textSize="@dimen/text_size_12"
            android:textStyle="bold" />

    </LinearLayout>

    <ImageView
        android:id="@+id/community_listitem_collection"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:src="@drawable/head_favorite" />

</LinearLayout>